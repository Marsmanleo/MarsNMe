{
  "name": "@marsnme/cloudflare-routing-worker",
  "private": true,
  "version": "0.0.1",
  "description": "Routing-layer worker for mcp.marsnme.com — username-based MCP proxy with setup page",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:safe": "./scripts/deploy.sh"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "wrangler": "^3.78.4",
    "@cloudflare/workers-types": "^4.20260529.0"
  }
}
